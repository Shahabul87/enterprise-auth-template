# Gmail SMTP Configuration for Email Testing
# Copy these settings to your .env file after filling in your details

# Email Configuration
EMAIL_ENABLED=true
EMAIL_FROM=your.email@gmail.com  # Replace with your Gmail address
EMAIL_FROM_NAME=Enterprise Auth System

# Gmail SMTP Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your.email@gmail.com  # Replace with your Gmail address
SMTP_PASSWORD=xxxx xxxx xxxx xxxx  # Replace with your 16-character App Password (remove spaces)
SMTP_USE_TLS=true

# Important Gmail SMTP Settings:
# 1. Use port 587 for TLS (recommended) or port 465 for SSL
# 2. SMTP_USER must be your full Gmail address
# 3. SMTP_PASSWORD must be the App Password, NOT your regular Gmail password
# 4. Remove spaces from the App Password when pasting

# Testing Email Addresses
# For testing, you can use:
# - Your primary Gmail address
# - Any Gmail aliases (yourname+test1@gmail.com, yourname+test2@gmail.com)
# - Other real email addresses you have access to

# Frontend URL for email links
FRONTEND_URL=http://localhost:3000

# Example App Password format (DO NOT USE THIS - GET YOUR OWN):
# Original from Google: abcd efgh ijkl mnop
# Use in config as: abcdefghijklmnop